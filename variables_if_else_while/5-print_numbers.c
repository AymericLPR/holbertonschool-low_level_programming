#include <stdlib.h>
#include <stdio.h>
/**
 * main - entry point
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
int nombre = 0
while (nombre < 10)
{
putchar(nombre);
nombre++;
}
putchar('\n');
return (0);
}
    
