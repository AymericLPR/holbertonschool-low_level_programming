#include <stdlib.h>
#include <stdio.h>
/**
 * main - entry point
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
int number = 0;
while (number < 10)
{
printf("%d",number);
number++;
}
putchar('\n');
return (0);
}
    
