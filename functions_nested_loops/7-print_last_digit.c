#include "main.h"
/**
 * print_last_digit - Entry point
 * Return: 0 (Success)
 * @ld: last digit
 */
int print_last_digit(int ld);
{
int n;
if (ld % 10 > 0)
{
n=((ld % 10) * -1)
}
printf(%d, ld % 10);
return (%d);
}
