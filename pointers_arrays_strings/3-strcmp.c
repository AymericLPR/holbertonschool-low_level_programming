#include "main.h"

/**
 * _strcmp - compares two given strings
 * @s1: string
 * @s2: string
 * Return: Always 0 
 */
int _strcmp(char *s1, char *s2)
{
int x;

while(s1[i] == s2[i])
i++;
return (s1[i] - s2[i]);
}
