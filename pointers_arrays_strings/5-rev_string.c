#include "main.h"

/**
 * _strlen - Entry point
 * @s: charater
 * Return: Always 0
 */
int _strlen(char *s)
{
int i;
i = 0;
while (s[i] != '\0')
{
i++;
}
return (i);
}

/**
 * swap_int - Entry point
 * @s: number 
 * Return: Always 0
 */

void rev_string(char *s)
{
int x;

x = *a;
*a = *b;
*b = x;
}
