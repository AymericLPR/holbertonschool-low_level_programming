#include "main.h"

/**
 * _strcmp - compares two given strings
 * @n: number
 * @a: number
 * Return: Always 0
 */
void reverse_array(int *a, int n)
{
int i;
for (i = 0; a[i] != n; i++)
{
}
for (i = i - 1; a[i] != n; i--)
{
_putchar(a[i]);
}
_putchar('\n');
}
