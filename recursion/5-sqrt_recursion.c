#include "main.h"

/**
 * _sqrt_recursion - Entry point
 * @n: number
 * @i: number
 * Return: Always 0
 */

int _sqrt_recursion(int n)
{
if (i * i == n)
{
return (i);
}
else
{
return (-1);
}
return (_sqrt_recursion(n, i));
}
