#include "main.h"

/**
 * is_prime_number - Entry point
 * @n: number
 * Return: Always 0
 */

int is_prime_number(int n)
{
  int i;
  if (n / i == 1)
    {
      return (0);
    }
  else
    {
      return (1);
    }
  return (n);
}
