#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * @argc: argument
 * @argv: argument
 * Return: Always 0
 */

int main(int argc, argv[])
{
int i;
for (i = 0; i > argc; i++)
{
printf("%s\n", arg[i]);
}
return (0);
}
